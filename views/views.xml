<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="telegram.list">
      <field name="name">telegram list</field>
      <field name="model">telegram.telegram</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="telegram.action_window">
      <field name="name">telegram window</field>
      <field name="res_model">telegram.telegram</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="telegram.action_server">
      <field name="name">telegram server</field>
      <field name="model_id" ref="model_telegram_telegram"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="telegram" id="telegram.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="telegram.menu_1" parent="telegram.menu_root"/>
    <menuitem name="Menu 2" id="telegram.menu_2" parent="telegram.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="telegram.menu_1_list" parent="telegram.menu_1"
              action="telegram.action_window"/>
    <menuitem name="Server to list" id="telegram" parent="telegram.menu_2"
              action="telegram.action_server"/>
    -->
  </data>
</odoo>